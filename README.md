# Knowledgebase Agent App

## Local Development

```bash
pnpm install
pnpm dev
```

The app starts with the mock model provider until an API key is saved in Settings. No real key is required for tests or first-run development.

## Verification

```bash
pnpm qa
```
